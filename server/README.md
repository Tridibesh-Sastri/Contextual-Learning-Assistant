# Learning Assistant Proxy

For the full implementation guide and app usage, see the root README.

## Setup
1. Copy .env.example to .env and update values if needed.
2. Install dependencies: npm install
3. Run: npm run dev

## Endpoints
- POST /api/ask
- GET /health
